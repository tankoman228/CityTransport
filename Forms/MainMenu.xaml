<Window x:Class="CityTransport.Forms.MainMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CityTransport.Forms"
        mc:Ignorable="d"
        Title="MainMenu" MinHeight="500" MinWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*" MaxHeight="400" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="tbl_header" 
            Grid.ColumnSpan="3" TextAlignment="Center" VerticalAlignment="Center">
            City Transport
        </TextBlock>

        <Frame x:Name="pgContents" Grid.Row="1">
            
        </Frame>
        
    </Grid>
</Window>
