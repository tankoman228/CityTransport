<Window x:Class="CityTransport.Forms.RouteCreate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CityTransport.Forms"
        mc:Ignorable="d"
        Title="RouteCreate" Height="400" Width="400" ResizeMode="NoResize">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="10">
            <TextBlock FontSize="32">Choose the carrier</TextBlock>
            <ComboBox  x:Name="cbCarrier"
                FontSize="32"></ComboBox>
            <TextBlock FontSize="32">Choose transport type</TextBlock>
            <ComboBox  x:Name="cbType"
                FontSize="32"></ComboBox>
            <TextBlock  FontSize="32">Route Number:</TextBlock>
            <TextBox  x:Name="tbRouteNumber"
                FontSize="32"></TextBox>
            <TextBlock  FontSize="16"></TextBlock>
            <Button x:Name="btnSave"
                FontSize="32">Create and save</Button>
        </StackPanel>
    </Grid>
</Window>
